import React from 'react';

const Hero = () => {
  return (
    <section className="hero">
        <div>
            <h1>Give what you don't need</h1>
            <p>A community-driven platform for sharing unused items with others in need.</p>
        </div>
    </section>
  );
}

export default Hero;